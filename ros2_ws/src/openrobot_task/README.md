# openrobot_task

Planned boundary for the `IDLE`, `SEARCH`, `ALIGN`, `APPROACH`, `ARRIVED`, and
`FAULT` states. Every stale, invalid, or unhealthy input must converge to zero
velocity. No runtime node is implemented yet.
