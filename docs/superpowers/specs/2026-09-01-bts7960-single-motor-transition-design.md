# BTS7960 单电机真机轨转向设计

## 1. 目标与结果边界

OpenRobot-One 保留已经验证的 ROS 2 双轮差速仿真轨，同时将当前真机轨永久缩减为单电机架空速度闭环验证台：

```text
仿真轨：/cmd_vel -> Gazebo 双轮差速 -> /odom、TF、/scan -> SLAM

真机轨：PC 串口工具 -> STM32F407 -> IBT-2/BTS7960
        -> 单台 JGA25-370 -> AB 编码器 -> STM32 单电机速度闭环
```

当前真机轨不实现或宣称：

- 双电机差速控制；
- 落地直行或原地旋转；
- 真机 `/odom`、`joint_states` 或 `odom -> base_footprint` TF；
- 真机 SLAM、AMCL 或 Nav2；
- 未经实测的编码器计数、电流、稳态误差和停车时间。

双电机底盘只作为远期扩展，不属于当前里程碑或验收条件。

## 2. 已购硬件与证据等级

当前方案围绕以下硬件整理：

- STM32F407ZGT6 开发板；
- ST-Link V2；
- CH340 USB-TTL；
- 一台 JGA25-370、12 V、620 RPM、带 AB 编码器的电机；
- 一块 IBT-2/BTS7960 H 桥模块；
- 12 V / 5 A 适配器及配套 DC 转接线；
- 连接线和端子材料。

本轮三张 IBT-2 商品截图只证明卖家页面的标注，不证明到货模块的芯片真伪、连续电流、逻辑兼容性、排针方向或电气状态。整理时必须区分：

- `照片确认`：图片中可直接辨认的型号、丝印和端子名称；
- `卖家标称`：电压、输入电平、PWM 频率和 43 A 等商品参数；
- `实测确认`：由万用表、示波器、烧录结果或受控实验取得的数据；
- `UNKNOWN`：尚无可靠证据的参数。

三张商品图存在两项必须保留的冲突：

1. 动力电源范围同时写为 `5.5–27 V` 和 `6–27 V`；12 V 位于两者交集，但不能据此确认模块全范围。
2. 两张图对 `RPWM`、`LPWM` 的正反转语义互相颠倒；软件方向只能通过架空低能量脉冲实测冻结。

商品页所称 74HC244 “隔离”只按逻辑缓冲记录，不解释为光耦或隔离电源意义上的电气隔离。STM32 与模块仍需要共地。`43 A` 不作为连续能力或小电机保护依据。

## 3. 当前架构

### 3.1 仿真轨

仿真轨保持现有设计和发布职责：

- Gazebo 差速插件订阅 `/cmd_vel`；
- Gazebo 发布 `/odom` 和 `odom -> base_footprint`；
- `robot_state_publisher` 发布机器人内部 TF；
- SLAM Toolbox 或 AMCL 二选一发布 `map -> odom`。

现有 URDF/Xacro、Gazebo、SLAM、Docker、CI、检查脚本和测试均保留。它们是已验证成果，不因真机硬件缩减而删除。

### 3.2 单电机真机轨

当前单电机台架不作为 ROS 底盘执行端。最小数据流为：

```text
PC 测试工具
  -> UART 目标转速/停止/心跳
  -> STM32 安全状态机
  -> RPWM/LPWM/R_EN/L_EN
  -> IBT-2/BTS7960
  -> 单台电机
  -> AB 编码器
  -> STM32 计数、RPM、PID
  -> UART 状态/故障/遥测
```

ROS `hardware` Launch 继续作为安全占位入口，不发送任何运动命令。后续若增加 ROS 台架节点，只允许使用 `target_rpm`、`motor_state` 和诊断语义，不发布底盘里程计或 TF。

## 4. 候选 STM32 资源

以下仅是经板卡引出和复用表初筛的候选资源，必须在 CubeMX、实物丝印和无动力电平测试后冻结：

| 功能 | 候选引脚 | 说明 |
| --- | --- | --- |
| `RPWM` | `PB6 / TIM4_CH1` | PWM，启动比较值为 0 |
| `LPWM` | `PB7 / TIM4_CH2` | PWM，启动比较值为 0 |
| `R_EN` | `PB8` | 普通输出，启动为低 |
| `L_EN` | `PB9` | 普通输出，启动为低 |
| 编码器 A | `PA0 / TIM2_CH1` | 编码器模式候选 |
| 编码器 B | `PA1 / TIM2_CH2` | 编码器模式候选 |

`PA9/PA10` 保留给 USART1，`PA13/PA14` 保留给 SWD，`PC13` 保留给状态 LED。`R_IS/L_IS` 首次实验不连接；其比例、最大电压和 STM32 接口方案保持 `UNKNOWN`。

外部硬件需要保证 MCU 复位窗口内使能为低，不能只依赖应用代码初始化。

## 5. 安全状态与实验阶段

### H0：装备与机械安全

- 电机可靠固定并架空，不装轮子；
- 人员现场值守，旋转轴周围无松散物；
- 万用表、2 A 保险丝、直流断电开关或急停可用；
- 首次使用可调限流电源，或按受限方案增加串联 12 V / 21 W 灯泡；
- 动力线、端子和绝缘满足电流与机械固定要求。

### H1：断电电气核查

- 按实物逐线确认 `B+`、`B-`、`M+`、`M-` 和控制排针；
- 测量动力输入无明显短路，确认极性和公共地；
- 确认模块 `VCC` 要求、STM32 3.3 V 高电平兼容性和控制脚实际电平；
- 确认编码器供电和 A/B 输出不会超出 STM32 输入范围；
- 不以芯片或商品页内部保护替代外部保险丝和限流。

### H2：无动力控制验收

- 不连接电机动力电源；
- 上电、复位、Fault 和错误处理路径均保持 `R_EN=L_EN=0`、PWM 比较值为 0；
- 验证现有 UART H2 基线仍通过；
- 当前 500 ms UART 半包清理不作为电机命令看门狗。

### H3：单次受控脉冲与开环

- 只允许一次授权的单方向 `5% / 100 ms` 脉冲；
- 使用非阻塞定时到期停车，不依赖串口轮询返回；
- 到期、串口断开、错误、MCU 复位和人工停止均进入统一安全停车；
- 方向语义由实测建立，不照抄冲突的商品图；
- 通过后才逐级进入低占空比开环 RPM 测试。

### H4：编码器与闭环

- 手转输出轴三次标定每圈计数，离散度不高于 1%；
- 冻结编码器方向和电机方向符号；
- 100 Hz 速度采样与单电机 PID 不持续振荡；
- 三个受控目标点稳态误差不高于 10%；
- 有效控制命令超过 500 ms 未更新即停车；
- 所有指标必须来自真实日志，不使用示例值。

任一安全门失败，后续非零输出停止，状态保持 `BLOCKED`。

## 6. 协议边界

当前真机协议只覆盖单电机：

- `SET_MOTOR_RPM`：设置受限目标转速；
- `STOP`：立即进入安全停车；
- `HEARTBEAT`：刷新有效命令看门狗；
- `MOTOR_STATE`：返回计数、RPM、目标、输出和状态位；
- `FAULT`：返回协议、电气或控制故障。

CRC、字节序、载荷长度和测试向量在 PC/STM32 逐字节一致后冻结。原左右轮消息和差速运动学移入远期扩展，不作为当前实现条件。

## 7. 文档和资产迁移

### 7.1 更新当前有效文档

同步修改：

- `AGENTS.md`；
- `README.md`；
- `docs/architecture.md`；
- `docs/hardware_bom.md`；
- `docs/hardware_facts.md`；
- `docs/roadmap.md`；
- `docs/stage1_tasks.md`；
- `docs/acceptance.md`；
- `docs/protocol.md`；
- `docs/project_showcase.md`；
- `firmware/openrobot_firmware/README.md`；
- ROS hardware Launch 的安全占位提示。

### 7.2 整理新图片

保存原始商品截图为：

```text
硬件参数图片/bts7960/
├── IBT-2-商品参数.jpg
├── IBT-2-排针与动力端定义.jpg
├── IBT-2-使用方法与模块结构.jpg
└── README.md
```

图片索引记录来源、获取日期、SHA-256、可辨认内容、卖家声明、相互冲突和 `UNKNOWN`。README 展示副本使用清晰英文文件名，并明确它是商品资料而非实物验收照片。

### 7.3 删除失效内容

在引用更新后删除：

- `硬件参数图片/drv8871/`；
- `硬件参数图片/TB6612正面.jpg`；
- `硬件参数图片/TB6612背面.jpg`；
- `docs/assets/showcase/drv8871-closeup.jpg`；
- `docs/superpowers/specs/2026-09-01-no-bench-safety-hardware-experiment-design.md`；
- `docs/verification/2026-09-01-fastest-real-hardware-path.md`，由 BTS7960 单电机指南替代；
- 两份固定为旧驱动/双电机方案的 Word 手册；
- 只用于生成旧手册的 `tools/build_beginner_handbook.py`。

构建目录、缓存和临时渲染结果只在确认位于工作区且被忽略后清理；不触碰用户工作树或工作区外文件。

### 7.4 保留历史证据

以下内容不因硬件转向删除：

- 带日期的 H2、UART、ROS 和 Gazebo 实测报告；
- H2 UART 固件和 PowerShell 验收脚本；
- ROS 2 包、URDF/Xacro、Gazebo、SLAM、Docker 和 CI；
- STM32、ST-Link、CH340 和电机原始照片。

历史报告可增加“后续方案已替换”的顶部说明，但不改写当日实测结果。

## 8. 验证与交付

文档和资产迁移完成后执行：

1. `git status`、`git diff --check` 和完整差异审查；
2. 检查当前有效文档没有把 DRV8871/TB6612 或双电机真机写成当前方案；
3. 检查所有 Markdown 图片和相对链接存在；
4. 运行仓库 Python/pytest 静态测试；
5. 在 Docker/ROS 2 Humble 可用时运行 `scripts/build_ros.sh`；
6. 重新记录包数和测试数，禁止沿用未复验统计；
7. 确认没有新增可产生非零电机输出的固件；
8. 提交 Git 并推送当前 `main` 到 `origin/main`；
9. 核对 GitHub 远端提交和 CI 状态。

推送失败、认证缺失或 CI 未完成时，必须明确报告为未验证或阻塞，不声称 GitHub 已更新。

## 9. 当前开放风险

- 尚无到货 IBT-2 模块正反面实物照片；排针方向和芯片顶标未确认。
- 12 V / 5 A 固定适配器没有可调限流；首次实验仍需要外部保护方案。
- 编码器电平、PPR/CPR、减速比折算计数和实际电机电流未实测。
- 商品页方向定义冲突，不能提前冻结软件正转语义。
- 当前固件只有 H2 UART，不具备安全电机输出能力。

这些风险不阻塞文档与资产转向，但继续阻塞任何电机通电和非零命令。
